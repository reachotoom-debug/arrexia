/**
 * UI-only layout tokens for authenticated pages.
 * Keep in sync with `PageContainer` variants.
 */
export const PAGE_WRAP_BASE =
  "mx-auto w-full min-w-0 px-4 py-4 sm:px-6 md:py-6 lg:px-8";

export const PAGE_MAX_DEFAULT = "max-w-[75rem]";
export const PAGE_MAX_TABLES = "max-w-[80rem]";
export const PAGE_MAX_NARROW = "max-w-[64rem]";
export const PAGE_MAX_WIDE_DETAIL = "max-w-[70rem]";

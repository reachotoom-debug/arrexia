/**
 * Standard list-page filter region — same building blocks as CommandBar, exported as FilterBar for consistency.
 */
export {
  CommandBar as FilterBar,
  CommandBarSearch as FilterBarSearch,
  CommandBarControls as FilterBarControls,
} from "./CommandBar";

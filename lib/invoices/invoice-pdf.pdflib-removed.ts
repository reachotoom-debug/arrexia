/**
 * Invoice PDFs were previously generated with pdf-lib in `pdf.ts`.
 * Superseded by `InvoicePdfDocument.tsx` and `generateInvoicePdf` using
 * `@react-pdf/renderer`. Kept as a breadcrumb; see git history for the old implementation.
 */

export {};
